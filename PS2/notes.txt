
For performance purposes, the results reported here were obtained without printing iters/distances. 
Versão do Julia, processador do computador, detalhes da paralelização...
Lesson: it is not wise to simply stack all "tricks" together... The code can become slower!


Separar em vários arquivos, padronizar comentários, botar tudo na forma "max{vetor}" e checar um a um. 

interpretacao dos euler errors, tabela com estatísticas etc.
import Pkg; Pkg.add("Interpolations")

Multigrid with vectorized maximization: discutir o problema de alocação de memória. 
Multigrid + concavity + monotonicity